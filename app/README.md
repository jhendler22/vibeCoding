# Olympic Hockey Stats Tracker

Single full-stack React + Vite + TypeScript app with integrated Express API routes for standings, players, live games, cache fallback, and CSV export.

## Run

```bash
npm install
npm run dev
```

## Build and start

```bash
npm run build
npm run start
```
